spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/a1424132610/config_server.git
          search-paths: ''
      label: master
  application:
    name: config-server
  profiles:
    active: dev