spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/a1424132610/Mirco-Services.git
          search-paths: config/config-server-config
  application:
    name: config-server
  profiles:
    active: dev